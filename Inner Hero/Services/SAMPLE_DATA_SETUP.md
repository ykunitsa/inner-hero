# Быстрая настройка тестовых данных

## ⚡ Быстрый старт

Тестовые данные уже настроены и будут загружаться автоматически при первом запуске в DEBUG режиме!

## ✅ Что уже сделано

1. ✅ Создан JSON файл с 8 примерами экспозиций (`SampleData.json`)
2. ✅ Создан загрузчик данных (`SampleDataLoader.swift`)
3. ✅ Интегрирована автоматическая загрузка в `Inner_HeroApp.swift`

## 📋 Что нужно проверить в Xcode

### Шаг 1: Добавить файлы в проект

Убедитесь, что новые файлы добавлены в проект Xcode:

1. Откройте проект в Xcode
2. В навигаторе проекта найдите папку `Services/`
3. Если файлы `SampleDataLoader.swift` и `SampleData.json` не видны:
   - Перетащите их из Finder в папку `Services/` в Xcode
   - Или: `File` → `Add Files to "Inner Hero"...`
   - Выберите оба файла
   - **Важно**: Поставьте галочку напротив "Inner Hero" в разделе "Add to targets"

### Шаг 2: Проверить Target Membership

Для файла `SampleData.json`:

1. Выберите файл в навигаторе проекта
2. Откройте правую панель (Inspector)
3. В разделе "Target Membership" должна стоять галочка напротив "Inner Hero"
4. Если галочки нет - поставьте её

### Шаг 3: Собрать и запустить

```bash
# В Xcode нажмите Cmd+B (Build)
# Затем Cmd+R (Run)
```

При первом запуске в консоли появится:
```
✅ Тестовые данные загружены успешно
```

## 🧪 Что будет загружено

### 8 экспозиций с разным уровнем сложности:

| Экспозиция | Сложность | SUDS | Длительность |
|-----------|-----------|------|-------------|
| Публичное выступление | 8 | 8 | 15 мин |
| Поездка в лифте | 5 | 6 | 5 мин |
| Звонок незнакомому | 6 | 7 | 10 мин |
| Знакомство в кафе | 9 | 9 | 20 мин |
| Прогулка в людном месте | 4 | 5 | 30 мин |
| Задать вопрос продавцу | 3 | 4 | 10 мин |
| Онлайн-встреча | 7 | 7 | 15 мин |
| Поход в кинотеатр один | 5 | 6 | 180 мин |

### Примеры сеансов (для каждой экспозиции):

- 3-5 завершенных сеансов
- Распределены за последние 2 недели
- С реалистичными данными SUDS
- С заметками

## 🔧 Управление данными

### Сбросить загрузку (загрузить данные заново)

```bash
# Удалить приложение с симулятора/устройства
# Или выполнить в коде:
UserDefaults.standard.removeObject(forKey: "hasLoadedSampleData")
```

### Отключить автоматическую загрузку

В файле `Inner_HeroApp.swift` измените:

```swift
#if DEBUG
let shouldLoad = false  // Изменить на false
#else
let shouldLoad = false
#endif
```

### Добавить кнопку ручной загрузки

В любом представлении с доступом к `modelContext`:

```swift
Button("Загрузить тестовые данные") {
    do {
        try SampleDataLoader.loadSampleExposures(into: modelContext)
        let exposures = try modelContext.fetch(FetchDescriptor<Exposure>())
        try SampleDataLoader.loadSampleSessions(for: exposures, into: modelContext)
    } catch {
        print("Ошибка: \(error)")
    }
}
```

## 🐛 Troubleshooting

### Ошибка "fileNotFound"

**Проблема**: JSON файл не найден в bundle приложения

**Решение**:
1. Выберите `SampleData.json` в Xcode
2. В правой панели проверьте "Target Membership"
3. Убедитесь, что стоит галочка напротив "Inner Hero"

### Данные не загружаются

**Проверьте**:
1. Консоль Xcode - должно быть сообщение о загрузке
2. `UserDefaults`: возможно данные уже были загружены
3. DEBUG режим: проверьте, что компилируете в Debug, а не Release

**Сброс**:
```swift
// Добавить временно в init() или onAppear:
UserDefaults.standard.removeObject(forKey: "hasLoadedSampleData")
```

### Дублирование данных

Загрузчик проверяет, что база данных пустая перед загрузкой. Если нужно очистить:

```swift
let dataManager = DataManager(modelContext: modelContext)
try dataManager.deleteAllData()
```

## 📝 Добавление своих данных

Отредактируйте `SampleData.json` и добавьте новую экспозицию:

```json
{
  "title": "Моя экспозиция",
  "description": "Описание",
  "expectedSUDS": 7,
  "difficulty": 6,
  "durationMinutes": 20,
  "steps": [
    "Шаг 1",
    "Шаг 2"
  ]
}
```

## 📚 Дополнительная информация

Подробное руководство: `SAMPLE_DATA_GUIDE.md`

